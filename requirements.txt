# MerCury Email Platform - Production Dependencies
# Pinned versions for stability and security
# Updated: 2024-12-14

# Core - Email sending and configuration
aiosmtplib==3.0.2
pyyaml==6.0.1
click==8.1.7
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Database - SQLAlchemy ORM and migrations
sqlalchemy>=2.0.36
alembic==1.13.1

# Web Framework - Flask with WebSocket support
# flask 3.0.0 -> 3.1.3 closes CVE-2026-27205.
# python-socketio 5.11.0 -> 5.14.0 closes CVE-2025-61765.
# flask-cors held at 4.0.2 (latest 4.x with patches PYSEC-2024-71
# and CVE-2024-6839/6844/6866) — the 6.0 jump is a major break and
# deferred to its own dep-refresh PR.
flask==3.1.3
flask-socketio==5.3.6
python-socketio==5.14.0
flask-login==0.6.3
flask-limiter==3.5.1
flask-cors==4.0.2
flask-wtf==1.2.1

# Production Server - Gunicorn with Eventlet for SocketIO
# gunicorn 21.2.0 -> 22.0.0 closes CVE-2024-1135 and CVE-2024-6827.
# eventlet 0.35.2 -> 0.40.3 closes CVE-2025-58068.
gunicorn==22.0.0
eventlet==0.40.3
# psycopg2-binary is only needed for PostgreSQL deployments — SQLite
# users can `pip install -r requirements.txt --no-deps` and skip it.
# 2.9.11 was the first version to publish cp314 wheels for Windows;
# pinning to 2.9.12 (latest 2.9.x) avoids build-from-source fallback
# on Python 3.13/3.14 hosts that don't have pg_config installed.
psycopg2-binary==2.9.12

# Security - Encryption and authentication
# cryptography 42.0.1 -> 46.0.7 closes CVE-2026-26007, PYSEC-2026-35,
# and PYSEC-2026-36 (44.x is unpatched for the first two; 46.0.6 still
# carries the third).
cryptography==46.0.7
passlib==1.7.4
bcrypt==4.1.2

# Document Generation - PDFs, DOCX, QR codes
qrcode[pil]==7.4.2
pillow==10.4.0
reportlab==4.0.9
python-docx==1.1.0
weasyprint==68.1

# Email Validation
email-validator==2.1.0.post1

# Async I/O - File operations
aiofiles==23.2.1

# Task Scheduling - Cron-like campaign scheduling
apscheduler==3.10.4

# HTTP Client - Webhooks and API calls
httpx==0.26.0

# Structured Logging
structlog==24.1.0
python-json-logger==2.0.7

# Observability - optional error tracking + metrics, activated by env.
# SENTRY_DSN turns on Sentry; GET /metrics is always served (gate it with
# METRICS_TOKEN or at your proxy). Both are import-guarded in web/, so a
# core install without these still boots.
sentry-sdk[flask]==2.63.0
prometheus-client==0.25.0

# CLI and Utilities
# python-dotenv 1.0.0 -> 1.2.2 closes CVE-2026-28684.
# tqdm 4.66.1 -> 4.66.3 closes CVE-2024-34062.
python-dotenv==1.2.2
tqdm==4.66.3
pysocks==1.7.1
rich==13.7.0

# Optional: Better performance
uvloop>=0.21.0; sys_platform != 'win32'
orjson>=3.10.0

# Platform directories (log/data paths cross-platform)
platformdirs>=4.0.0
